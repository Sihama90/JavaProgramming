package day08_IfStatements;

public class IdentifyNumber {

    public static void main(String[] args) {
      int number = 200;
      boolean positiveNum = number >= 0;
      boolean negativeNum = number <= 0;
      boolean zero = number == 0;

        System.out.println(number + " is positive number: " + positiveNum);
        System.out.println(number + " is negative number: " + negativeNum);
        System.out.println(number + " is zero: " + zero);

 /* Task:
    1. Create a class called IdentifyNumber, and write a program that can identify if the given number is positive,
       negative or zero.
        Ex:
                number = 200

            output:
                200 is positive number: true
                200 is negative number: false
                200 is zero: false

  */









    }







}
